// content.js

// Create a div element
const customDiv = document.createElement('div');
customDiv.id = 'customDivId'; // Set an id for the div (you can change this)
document.body.appendChild(customDiv);
